# Shared Dart package (placeholder)

Will hold shared Dart models + the API client used by both Flutter apps
(customer_app and delivery_app), so order/status/vendor types are defined once.

Built in Phase 1–2. Not yet a real pub package.
